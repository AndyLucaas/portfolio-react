/**
 * theme.js — theme identifiers. Color values themselves live in CSS
 * custom properties (presentation/styles/variables.css) so designers
 * can tune them without touching JS.
 */
export const THEMES = {
  DARK: 'dark',
  LIGHT: 'light',
};

export const DEFAULT_THEME = THEMES.DARK;
