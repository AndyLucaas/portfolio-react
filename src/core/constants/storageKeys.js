/**
 * storageKeys.js — centralised localStorage key names to avoid typos
 * and collisions across the app.
 */
export const STORAGE_KEYS = {
  THEME: 'portfolio:theme',
  LOCALE: 'portfolio:locale',
};
